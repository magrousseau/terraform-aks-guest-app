apiVersion: v1
kind: Service
metadata:
    name: magrousseauterraformaksguestapp
spec:
    type: LoadBalancer
    ports:
    - port: 5000 
    selector:
        app: magrousseauterraformaksguestapp